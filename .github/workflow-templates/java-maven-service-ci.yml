name: Java Maven service CI

on:
  push:
    branches:
      - $default-branch
  pull_request:
    branches:
      - $default-branch

permissions:
  contents: read

jobs:
  maven-verify:
    uses: digital-bank-java/.github/.github/workflows/java-maven-verify.yml@691ebfdb867d97bdff42f4a1b238cf9802b67caf
    permissions:
      contents: read
